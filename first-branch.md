# First Branch

This file was created on `feature/first-merge` and merged into `main`.

Run the following command to see the branch and merge history:

```bash
git log --oneline --graph --decorate --all
```
